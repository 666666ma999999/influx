# TOB/MBO ディールポリシー `tob_drift_v1` — 前向き事前登録（凍結申請 v2・2026-07-27）

> **位置づけ（第19R敵対レビュー裁定・B14路線）**: 新しい目標制度（§0-B等）は作らない。
> **単一の完全指定ポリシーを「1仮説」として前向き登録**し、信任は前向きペーパートレードのみで取る。
> 発見に使った歴史測定（2016-07〜2026-07・holdout込み）は**候補選定専用**であり信任に使わない。
> v2 = Codex凍結レビューR1（NO-GO・7ブロッカー・threadId 019fa25e）の全反映版。

## 1. 仮説（1本だけ）
**「TOB/MBO対象銘柄は、発表を市場が消化した後も買付価格への収斂ドリフトが残り、
発表後の翌々営業日寄付から20営業日の保有で、コスト後EVが正になる」**
- 歴史実測（候補選定用・信任外）: n=823ディール・EV+2.29%・片側95%下限+1.46%・上位10除外+1.28%・
  10/11年プラス・σ=13.8%/件。探索履歴（winner選択）: §8-5名人収穫→TDnet表題15イベント測定→deal監査。

## 2. シグナル定義（完全指定・R1ブロッカー3対応）
- **正規化関数（凍結）**: 表題は `unicodedata.normalize("NFKC", title)` 後、空白・改行を除去して判定。
  大文字小文字は NFKC 後にそのまま（regexは大小両対応で記述）。表題欠損＝非該当。
- **regex（文字列として凍結・評価順も固定）**:
  1. `TOB_ANY = 公開買付|ＭＢＯ|MBO|マネジメント・バイアウト`（NFKC後は `公開買付|MBO|マネジメント・バイアウト`）
  2. `SELF = 自己株` … TOB_ANYマッチかつSELFマッチ→**自己株TOB＝除外**
  3. `WITHDRAW = 撤回|中止|不成立|買付.*行わない|見送り` → 除外（かつdealの破談フラグ）
  4. `PROGRESS = 結果|終了|状況|応募|訂正|変更|延長|経過` → 除外（経過系）
  5. `QUALIFY = 開始|意見表明|賛同|実施|ＭＢＯ|MBO|マネジメント・バイアウト` → 上記1〜4を通過した上でマッチ＝**適格**
  評価順: TOB_ANY→SELF→WITHDRAW→PROGRESS→QUALIFY（全て NFKC 後の表題に適用）。
- **deal_id（凍結）**: 銘柄コード（5桁正規化）単位で、**TOB_ANYマッチ全開示**（除外開示も含む）を時系列に並べ、
  直前のTOB_ANY開示から**暦日差 > 90日**（`>90`・91日以上）なら新deal。窓の更新はTOB_ANY全開示が行う。
- **エントリー対象**: deal内で**時系列最先行の適格開示1件のみ**（dealの開始が除外開示でも、その後の
  最先行適格開示がシグナル。同一dealで2度目以降の適格開示ではエントリーしない）。
- **シグナル日T（凍結）**: 開示時刻はTDnetインデックスの公式 pubdate（JST・分粒度）。
  `pubdate < 15:00:00` → T=開示日D／`pubdate >= 15:00:00`（15:00:00ちょうどを含む）または D が非営業日 → T=Dの翌営業日。
- **境界ケースのgolden test**: 実装時に代表12ケース（15:00丁度・全半角MBO・90日丁度・91日・
  除外開示が窓を更新するケース等）のテストを同梱し、テストファイルのsha256を凍結記録に含める。

## 3. PIT運用（R1ブロッカー4対応）
- **日次ラン（凍結）**: 毎朝 **07:15 JST**（寄付前）に `tdnet_index_fetch.py --recent 7` を実行し、
  **その時点で観測済みの開示のみ**でシグナル判定。判定時刻・原レスポンスsha256を追記専用で記録。
- **注文締切**: 07:15ラン＝当日寄付にエントリーする唯一の機会。API遅延等で締切後に観測された
  過去開示は **`operational_missed` として記録し、遡及エントリーしない**（--recent 7 は欠測の可視化用）。
- 副系統: EDINET公開買付届出書との日次突合で欠測検知（シグナル定義には使わない・運用監視のみ）。

## 4. 執行仕様（ペーパー・R1ブロッカー5/6対応）
- **これは日足価格による仮想約定である**（板・特別気配は観測しない）ことを明記。
- **約定状態表（凍結）**: T+1のbarについて
  | 条件 | 状態 |
  |---|---|
  | `AdjO>0 ∧ Vo>0` | 成立（entry=AdjO） |
  | `Vo=0`（寄らず・売買停止） | **不成立** `unfilled_no_volume` |
  | bar欠損 | **不成立** `unfilled_no_bar` |
  | AdjO≤0/欠損 | **不成立** `unfilled_bad_price` |
  不成立は理由コード付きで台帳記録（判定のn外・別掲。感度分析としてT+2寄付再試行を別列記録＝判定外）。
- **検定母集団（凍結・R1ブロッカー5の解消）**: **合格判定は「成立した全deal」を1 deal=1観測の等ウェイトで行う**。
  同時保有上限・資金制約は**判定に用いない**（下記の参考ポートフォリオ列でのみ適用）。
  ＝どのdealを「選ぶ」自由度が判定に入らない。
- **参考ポートフォリオ列（判定外・運用感覚の把握用）**: 固定想定元本1,000万円・1件=min(元本の1/7,
  T+1売買代金×10%)・単元株丸め・同時8件目以降は「開示時刻昇順→コード昇順」で機械的に見送り・
  出口資金は翌営業日から再利用・リバランスなし。
- 出口: **20営業日後の終値**（成立日から起算）。期中売買停止・上場廃止＝窓内最終取引終値で清算（censored計数）。
  応募・決済持ち込みはしない。コスト: 往復0.3%控除（net=AdjC/AdjO−1−0.003・二重控除なし）。

## 5. 判定計画（R1ブロッカー1/2/7対応・逐次検定なし・延長なし）
- **α・台帳の前向き登録（凍結時に実施）**: 凍結と同時に trials.jsonl へ
  `status=registered_forward`（kpi_name=tob_drift_v1・α消費予約1）を**1行append**。
  以後、**成功・早期失敗・判定不能のいずれで終わってもこの1仮説はα消費済みの確定行として閉じる**
  （選択的報告の禁止。中止で分母から消えることはない）。
- **中間チェック（開始12ヶ月後・1回のみ・継続/早期失敗のみ・合格判定はしない）**:
  以下のいずれかで**早期失敗（α消費・終了）**:
  (a) 成立dealのnet単純平均 < 0（成熟分のみ）
  (b) deal系列累積和（等ウェイト1単位/deal・時系列）の最大ドローダウン < −15%
  (c) 直近3ヶ月の月平均適格deal数 < 3件
- **最終判定（唯一・延長なし）**: **開始24ヶ月後 または 成立deal n=150 到達の早い方**
  （ただし最低12ヶ月経過後）。結果は 合格／不合格 の2値のみ。**「判定不能→延長」は存在しない**
  （検出力不足で不合格となるリスク＝真のEVが歴史の2/3以下なら検出できない、を**受容した上での賭け**と明記）。
- **合格条件（AND・凍結）**:
  (1) 成立deal net の**片側95%CI下限 > 0**
  (2) **上位5deal除外後の平均net > 0**（正式なAND条件・宝くじ排除）
- **CI算出（凍結）**: シグナル月でクラスタ化した月次ブロック・ブートストラップ。
  n_boot=10,000・seed=**20260727**・percentile法・空月は除外・不成立dealはn外。
  実装コードのsha256を凍結記録に含める。
- 再挑戦: 本仮説が閉じた後の再登録は、非重複の将来期間・新規仮説・新規αでのみ可。

## 6. 監視条件（拒否権・合格条件ではない）
単日損失・破談発生時実損・銘柄/業種集中・第三者API欠測日数（3営業日連続で欠測→一時停止しユーザー報告）。
前向きでは破談・条件変更・対抗TOBを本文（31日以内は取得可）またはEDINETで監査する。

## 7. 変更管理
本仕様は凍結後変更禁止（regex・窓・時刻境界・判定点・seedを含む）。変更＝本仮説の放棄（α消費済みで閉じる）
＋新規事前登録。

## 8. Codex凍結レビューへの明示事項（R1から継続）
(a) 歴史測定はholdout込み＝信任は前向きのみ (b) 破談検出1.1%は表題マッチの下限値 (c) 日足による仮想約定
（板未観測・§4で明記） (d) 検出力の限界: 真のEV+2.29%なら必要n≒98・+1.5%なら228＝24ヶ月/150件では
減衰時に検出不能→**延長せず不合格で閉じる設計を選択** (e) 第三者API単一依存はEDINET副系統＋欠測監視で
緩和（完全解決ではない） (f) R1の7ブロッカーへの対応: §2正規化/regex文字列/deal_id境界/golden test・
§3 07:15締切とoperational_missed・§4状態表と検定母集団の一意化・§5 α前向き登録/早期失敗もα消費/
延長廃止/bootstrap完全指定・上位5除外の正式AND化。
