{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "profile_path": {
      "type": "string",
      "default": "./x_profile",
      "description": "Path to browser profile with authenticated cookies"
    },
    "dry_run": {
      "type": "boolean",
      "default": true,
      "description": "If true, simulate posting without actually posting to X"
    },
    "max_retries": {
      "type": "integer",
      "default": 3,
      "description": "Maximum retry attempts for failed posts"
    }
  },
  "additionalProperties": false
}
