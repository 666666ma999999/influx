# §7-AG-v2 改定ドラフト（DRAFT_PRE_CODEX_REFREEZE・2026-07-21）

**位置づけ**: §7-AG（決算跨ぎ回避オーバーレイの前向きシャドー観察・2026-07-19凍結 SHA=f8fe10d2…）の
実装前スキーマ確認（凍結時の宿題）で発見された「凍結仕様 vs 実API」の構造的不一致に対する改定。
ユーザー裁定A（2026-07-21「作り直して再凍結」）に基づく。**本改定はCodex再凍結GOまで無効・現行v1凍結が正**。

## 0. 改定を強制する実データ事実（2026-07-21確認・全て一次ソース）

- F1. earnings_calendar 実スキーマは7キーのみ（Date/Code/CoName/FY/SectorNm/FQ/Section）。
  **「予定未定」を示すフィールドは存在しない** → v1状態(5) explicit_no_schedule は恒久的に発生不能。
- F2. APIは**翌営業日発表予定のみ**を返す（docs/jquants-v2-api-map.md L33「翌営業日発表予定の全銘柄が
  返る仕様・過去遡及不可」・実測: 7/19(日)取得→Date=7/21のみ3件・7/21(火)日中取得→Date=7/21のみ3件）。
  → v1のE確定規則（シグナル時点snapshot単発でD+2〜20営業日窓のEを判定）では、シグナル時点に知り得る
  Eは翌営業日分のみ＝ほぼ全行 code_not_found→skip→**有効pair≈0で12ヶ月ゲート（月≥9・累計30pair）が
  構造的に不成立**。
- F3. 視程の切替タイミング実測: 営業日Xの日中取得でDate=X（当日分）。前営業日夜の更新で翌営業日分へ
  切替わる → **Xの朝（daily_screen 07:32編入済み・2026-07-21 commit a91272d）に取得すれば、開場
  （09:00）前に「X当日の発表銘柄」を知れる**＝ライブ執行可能かつPIT-clean。
- F4. 対象は**3月期・9月期決算会社のみ**（api-map L33明記・実測2スナップショットのFY値も3月31日/9月30日のみ）。

## 1. 変更点（この節に列挙した条項のみ差し替え・他は v1 凍結を全て継承）

### 1-1. E確定規則の置換（v1「シグナル時点snapshot単発」→ v2「保有窓の日次観測」）
保有窓の各営業日Xについて「Xの営業日朝までに取得された最新snapshot（fetched_at < X 09:00 JST、
実運用=daily_screen 07:32編入分）に当該コードがDate=Xで掲載されているか」を日次判定する。
E = 窓内で最初に掲載が観測された営業日。

### 1-2. シャドー規則v2
- **E = entry_date 当日**: shadow_no_entry（shadow_return=0%・コスト0）— v1と同義
- **entry_date < E < primary exit日**: **E当日の寄付（AdjO）でクローズしたとみなす**（entry価格=実
  エントリー価格・往復コスト込み・調整係数は既存Canonical）。
  変更理由: v1の「E直前営業日の終値」は、Eを知れるのが前営業日夜（大引け後）のため**執行不能**と判明。
  E当日寄付なら朝の知識で執行可能。保守性: 本邦決算開示は原則15時以降（引け後）のため寄付exitで
  跨ぎ回避が成立。日中開示の例外も寄付exitなら回避できる。
- **E が primary exit 日以降 or 窓内掲載なし**: オーバーレイ非適用（shadow=実リターン）— v1踏襲。
  v1の explicit_no_schedule 分岐は**廃止**（F1により恒久発生不能・かつ日次全数観測により「窓内
  発表なし」は推定でなく確定情報になるため不要）。
- 価格欠損・取引停止・entry_missing の扱いは v1 踏襲。
- -8%タッチ判定: shadow側=entry〜shadow exit（=E前日引けまでの安値。E当日寄付exitのため
  E当日の安値は含めない）／actual側=v1踏襲。+20%到達=§0定義に一致（v1踏襲）。

### 1-3. 状態分類v2（機械判定・優先順位・黙った補正禁止）
1. **coverage_gap**: 窓内（entry_date〜min(E, primary exit日)の各営業日）に有効snapshot欠測日が
   1日でもある → skip（有効pairにしない・理由別件数を月次報告）
2. **out_of_scope**: 当該銘柄が3月期/9月期決算でない（シグナル時点までにPITで入手可能な直近
   fins開示の決算期末で機械判定・判定不能も本状態=保守側）→ **主解析の分母外・診断のみ**。
   理由: F4によりAPIが本状態の銘柄を掲載しないため、「掲載なし」が「発表なし」を意味しない。
   有効pairに入れると paired差が構造的に0へ希釈され estimand が壊れる。
3. shadow_no_entry / 4. overlaid（E当日寄付クローズ） / 5. untouched（窓内発表なし or E≥primary exit日）
- 3〜5 が有効pair。月次報告必須: 状態別件数・out_of_scope率・snapshotのFY値分布
  （APIの対象決算期が将来拡大した場合の検知。拡大時の scope 変更は再改定=再凍結プロセス）。

### 1-4. PIT証跡v2
各shadow行に「窓内全営業日の (date, snapshot_file, sha256) マニフェスト」のsha256を保存
（マニフェスト実体は月次CSV隣接の manifest_YYYYMM.json・tmp書き→原子rename）。
snapshot事後差替え禁止・再計算時の当初snapshot再使用は v1 踏襲。

### 1-5. 窓開始
prospective_start_date = **2026-08-01**（初営業日8/3・12ヶ月固定窓・後日変更不可は v1 踏襲）。
条件: 7月末までに daily_screen 編入分の snapshot 取得が営業日連続で成功していること
（失敗日があれば開始を翌月初へ機械繰下げ・恣意選択なし）。

## 2. 変更しないもの（v1凍結のまま）
非介入の三層保証（コード/表示/運用）・12ヶ月起草権ゲート（最低標本 月≥9/累計pair≥30・通過条件
①〜④・延長禁止・KPI別採否禁止）・run_log証跡（hash chain・flock・no-op/FATAL規則）・
台帳1行（kpi_name=earnings_avoidance_overlay_shadow_v1・alpha_consumed=false・
verdict=reference_observation）・対象7系統の固定列挙・§7-E B1との整理。

## 3. 正直な注記
- 主解析母集団は「7系統confirmed × 3月期/9月期決算銘柄」に縮む。最低標本未達なら v1 どおり
  不通過（延長禁止）。out_of_scope率が高く構造的に標本不足なら、それ自体を12ヶ月後に正直に報告。
- 「E当日寄付exit」は v1「E前日終値exit」より跨ぎ回避としては同等（発表は引け後主体）だが、
  E当日の日中値動きを取り込まない分だけ機会損失/回避益の測定値は v1 想定と異なる。
  これは執行可能性を優先した意図的な仕様。
